import {createApp} from 'vue';import ElementPlus from 'element-plus';import * as Icons from '@element-plus/icons-vue';import 'element-plus/dist/index.css';import './style.css';import App from './App.vue';import {installHostState} from './bridge/jcefBridge';const app=createApp(App);Object.entries(Icons).forEach(([k,v])=>app.component(k,v));app.use(ElementPlus);app.mount('#app');installHostState()
